expr$ = input("Enter a math expression:", "Math")
if @status then
    exit
end if
ans$ = eval(expr$)
if @status then
	msgbox(48, "The expression you entered doesn't seem valid.")
else
	MsgBox(64, "The answer is:\n" + ans$, "Math")
end if
